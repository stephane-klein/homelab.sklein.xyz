[tasks.deploy]
description = "Deploy toggl-pg-mirror (syncs Toggl time entries to PostgreSQL)"
run = "scripts/deploy.sh"

[tasks.destroy]
description = "Destroy toggl-pg-mirror"
run = "scripts/destroy.sh"

[tasks.diff]
description = "Show the helm diff for toggl-pg-mirror and report drift"
run = "scripts/diff.sh"